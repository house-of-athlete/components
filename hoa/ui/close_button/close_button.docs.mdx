---
description: "A close button (✖️)."
labels: []
---

import { CloseButton } from "./close_button"

A close button. Set the color with the CSS variable `--hoa-close-button-color`.

```js live
<CloseButton
  onClick={() => {
    alert("Close")
  }}
/>
```
