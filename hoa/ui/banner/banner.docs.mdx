---
description: "Banner component."
labels: []
---

import { Banner } from "./banner"

A banner component. See compositions for examples.

In order to support the `content` prop, a `renderComponent` prop is required.
