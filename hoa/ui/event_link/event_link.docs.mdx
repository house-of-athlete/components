---
description: "A link for use with onClick events."
labels: []
---

import { EventLink } from "./event_link"

A link for use with `onClick` events. `preventDefault` will be called so you don't have to.

```js live
<EventLink
  onClick={() => {
    alert("🦄")
  }}
>
  Click me
</EventLink>
```
